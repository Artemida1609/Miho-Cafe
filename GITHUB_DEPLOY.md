# 📦 Інструкція: Завантаження проекту на GitHub

## Крок 1: Створіть репозиторій на GitHub

1. Відкрийте [GitHub.com](https://github.com) і увійдіть у свій акаунт
2. Натисніть кнопку **"+"** в правому верхньому куті → **"New repository"**
3. Заповніть форму:
   - **Repository name**: `miho_cafe` (або будь-яка назва)
   - **Description**: "Miho Cafe Website - Modern React + TypeScript website"
   - **Visibility**: Public (для GitHub Pages) або Private
   - **НЕ** ставте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Натисніть **"Create repository"**

## Крок 2: Ініціалізуйте Git в проекті

Відкрийте термінал (Git Bash, PowerShell або Command Prompt) в папці проекту:

```bash
cd "D:\Project X\coffee_shops\miho_cafe\website"
```

### 2.1. Ініціалізуйте Git репозиторій:
```bash
git init
```

### 2.2. Додайте всі файли:
```bash
git add .
```

### 2.3. Створіть перший commit:
```bash
git commit -m "Initial commit: Miho Cafe website"
```

## Крок 3: Підключіть до GitHub репозиторію

### 3.1. Додайте remote репозиторій:

Замініть `YOUR_USERNAME` на ваш GitHub username:

```bash
git remote add origin https://github.com/YOUR_USERNAME/miho_cafe.git
```

Або якщо використовуєте SSH:
```bash
git remote add origin git@github.com:YOUR_USERNAME/miho_cafe.git
```

### 3.2. Перевірте, що remote додано:
```bash
git remote -v
```

## Крок 4: Завантажте код на GitHub

### 4.1. Перейменуйте гілку на `main` (якщо потрібно):
```bash
git branch -M main
```

### 4.2. Завантажте код:
```bash
git push -u origin main
```

Якщо виникне помилка автентифікації, GitHub може запросити:
- **Personal Access Token** (якщо використовуєте HTTPS)
- Або налаштуйте SSH ключі

## Крок 5: Налаштуйте GitHub Pages

1. Перейдіть на GitHub у ваш репозиторій
2. Натисніть **"Settings"** (вгорі репозиторію)
3. У лівому меню знайдіть **"Pages"**
4. У розділі **"Source"**:
   - Виберіть **"GitHub Actions"** (рекомендовано)
   - Або **"Deploy from a branch"** → виберіть `main` → `/ (root)`
5. Натисніть **"Save"**

## Крок 6: Перевірте деплой

1. Після налаштування GitHub Actions автоматично запуститься
2. Перейдіть на вкладку **"Actions"** у вашому репозиторії
3. Дочекайтеся завершення workflow (зазвичай 2-3 хвилини)
4. Після успішного деплою сайт буде доступний за адресою:
   ```
   https://YOUR_USERNAME.github.io/miho_cafe/
   ```

## 🔧 Налаштування автентифікації (якщо потрібно)

### Якщо використовуєте HTTPS:

1. Перейдіть на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Створіть новий token з правами `repo`
3. Використовуйте token замість пароля при `git push`

### Якщо використовуєте SSH:

1. Створіть SSH ключ (якщо немає):
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
2. Додайте ключ до ssh-agent:
   ```bash
   eval "$(ssh-agent -s)"
   ssh-add ~/.ssh/id_ed25519
   ```
3. Скопіюйте публічний ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
4. Додайте ключ на GitHub: Settings → SSH and GPG keys → New SSH key

## 📝 Швидка команда для наступних змін

Після внесення змін у код:

```bash
git add .
git commit -m "Опис ваших змін"
git push
```

## ⚠️ Важливо

- Переконайтеся, що файл `.gitignore` правильно налаштований (не завантажуйте `node_modules`)
- Переконайтеся, що зображення скопійовані в `public/photos/` перед build
- GitHub Actions автоматично запуститься при кожному push в `main` гілку

## 🆘 Якщо щось пішло не так

1. **Помилка автентифікації**: Перевірте налаштування Personal Access Token або SSH ключів
2. **Помилка при push**: Переконайтеся, що remote URL правильний
3. **GitHub Pages не працює**: Перевірте вкладку "Actions" на наявність помилок
4. **404 на сайті**: Переконайтеся, що `base: '/miho_cafe/'` правильно налаштований у `vite.config.ts`

---

**Готово!** Ваш сайт тепер на GitHub! 🎉

